NOTE: this project is under development and it's not usable in any way so far.

Hydra is a POSIX compliant microkernel featuring memory mapped RPC.

Clone the repository with the `--recursive` flag to get the bootloader.
See docs/BUILDING for building instructions.
